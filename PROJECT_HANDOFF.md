# Franchise Insight Agent Handoff

이 문서는 Mac에서 작업한 내용을 Windows 컴퓨터나 다음 Codex 세션에서 바로 이어가기 위한 인수인계 메모입니다.

## 현재 목적

이 앱은 단순 키워드 조회기가 아니라, 프랜차이즈 양도양수 글감을 고르기 위한 인사이트 보드입니다.

핵심 질문은 아래입니다.

- 오늘 어떤 브랜드/지역 조합을 먼저 봐야 하는가?
- 단순 검색량 1등과, 최근 흐름이 평소와 달라진 이상 신호는 무엇인가?
- 선택한 브랜드가 지역별로 어디에서 반응이 있는가?
- 이 결과를 글쓰기 에이전트에 어떤 제목/방향으로 넘길 것인가?

## 현재까지 정리한 방향

## 2026-06-11 작업 종료 메모

오늘은 여기서 작업을 멈추고, 다음 작업은 Windows 환경에서 이어갈 예정입니다.

오늘 마지막으로 반영한 핵심 변경:

- 수도권 지도 배경 이미지를 추가하고, 그 위에 작은 지역 버튼을 배치
- 서울 25개 구는 모두 클릭 가능하게 변경
- 오산·안성·연천·가평·이천·광주·여주·양주 버튼은 제거
- 군포·용인 등 실사용 가능성이 있는 경기 주요 지역은 유지
- `분석된 지역 빠른 선택`은 화면에서 제거
- 지도 버튼 색상은 선택 브랜드/지역 조건의 지표 강도와 이상 신호를 보여주는 방향으로 유지

내일 Windows에서 가장 먼저 확인할 것:

- GitHub Desktop에서 최신 내용을 Pull
- `keyword-insight-agent/start-keyword-insight-agent.bat` 실행
- 브라우저에 표시되는 `localhost` 주소로 접속
- 지도 배경 이미지가 보이는지 확인
- 서울 25개 구 버튼이 모두 보이는지 확인
- 제거하기로 한 외곽 지역 버튼이 다시 보이지 않는지 확인
- `naver-api-config.json`은 GitHub에 올라가지 않으므로 Windows에도 별도로 준비

내일 바로 이어서 할 1순위 작업:

- 지도 버튼 위치를 실제 배경 지도에 더 자연스럽게 맞추기
- 서울 구 버튼이 너무 겹치거나 답답해 보이는 곳을 줄이기
- 브랜드 입력 후 `오늘 데이터 새로 수집`을 눌렀을 때 지도 버튼 색이 실제 분석 결과처럼 체감되는지 확인
- 선택한 지역 버튼을 눌렀을 때 오른쪽 설명과 아래 카드가 왜 그 지역을 봐야 하는지 더 명확히 말하게 만들기
- 검색량 1등 지역과 이상 신호 지역을 지도/목록에서 서로 구분해서 보이게 다듬기

내일 2순위로 고민할 것:

- 현재 지도는 배경 이미지 위에 버튼을 얹은 중간 단계입니다.
- 버튼 배치만으로 충분하면 이 방식으로 계속 가고, 행정구역 경계 자체를 색칠해야 하면 GeoJSON 지도 방식으로 전환 검토
- API 비용 때문에 `분석하기`는 저장 결과 확인, `오늘 데이터 새로 수집`은 실제 API 사용이라는 구분을 계속 유지
- 저장 결과가 없을 때 빈 지도처럼 보이지 않도록 안내 문구와 예시 상태를 더 친절하게 만들기
- Windows에서 포트가 바뀔 수 있으므로, 고정 주소를 외우지 말고 실행창에 표시되는 주소를 기준으로 열기

### 1. 필터는 검색창형으로 변경

이전에는 브랜드와 지역을 여러 개 칩으로 펼쳐 보여줬지만, 브랜드가 많아질수록 눈에 안 들어오고 필터 역할도 약했습니다.

현재 방향은 아래입니다.

- 브랜드 입력: 예) `메가커피`
- 지역 입력: 예) `서울 노원구`
- `분석하기` 버튼 또는 Enter
- 입력값이 없으면 시장 전체 기준으로 분석
- 브랜드만 입력하면 해당 브랜드의 지역별 흐름 분석
- 지역만 입력하면 해당 지역 안의 브랜드별 흐름 분석
- 브랜드+지역을 같이 입력하면 해당 조합 중심 분석

### 2. 검색량 상위와 이상 신호는 둘 다 유지

사용자 피드백상, 검색량 1등만 보거나 이상 신호만 보는 것은 부족합니다.

현재 화면은 `오늘 볼 신호`를 두 칸으로 나눕니다.

- 검색량 상위: 월검색량 자체가 큰 후보
- 이상 신호: 30일 평균 대비 최근 7일 흐름이 달라진 후보

둘 중 하나를 없애지 말고, 서로 다른 판단 근거로 유지해야 합니다.

### 3. 브랜드 수요 순위는 기본 시장 목록 기준

기존 기본 브랜드가 8개뿐이라 `TOP 10`이라고 해도 8개만 보였습니다.

현재는 기본 브랜드 목록을 24개로 늘렸고, 화면 제목도 실제 개수에 맞게 보이도록 정리했습니다.

예: `브랜드 수요 순위 (24개 중 상위 10개)`

### 4. 캐시 fallback 제거

중요한 수정입니다.

예전에는 조건에 맞는 캐시가 없으면 최신 캐시를 대신 보여줬습니다. 검색창형 필터에서는 이것이 위험합니다.

예를 들어 `메가커피 / 서울 노원구`를 입력했는데, 예전 전체 시장 캐시가 섞여 나오면 사용자가 필터가 안 먹는다고 느낍니다.

현재는 입력 조건과 정확히 맞는 캐시만 사용하고, 없으면 새로 수집합니다.

## 다음 작업 우선순위

### 1. 그래프 해석 개선

현재 그래프는 선만 보여줘서 변화 폭이 직관적으로 느껴지지 않습니다.

다음 개선안:

- 30일 평균 기준선 표시
- 최근 7일 구간 강조
- 마지막 값 라벨 표시
- 7일 평균, 30일 평균, 변화율을 카드로 분리
- `+25` 같은 숫자만 보여주지 말고 `30일 평균 대비 최근 7일 +18%`처럼 해석 문구 제공

### 2. 직접 검색 결과 상세 화면 개선

브랜드+지역을 입력했을 때 아래 정보가 한눈에 보여야 합니다.

- 핵심 키워드
- 월검색량
- 7일/30일 흐름
- 블로그 경쟁
- 왜 이 조합을 봐야 하는지
- 글쓰기 에이전트에 넘길 제목/방향

현재는 구조가 잡혔고, 설명 밀도와 시각화는 더 다듬어야 합니다.

### 2-1. 수도권 지도

2026-06-11 기준으로 전국 지도 대신 수도권 지도 패널을 추가했습니다.

현재 방향:

- 전국 지도는 불필요하므로 서울·경기·인천만 우선 표시
- 사이드바의 `분석된 지역 빠른 선택`은 제거
- 사용자가 준 수도권 지도 이미지를 `keyword-insight-agent/assets/capital-region-map-bg.png`로 저장하고 지도 배경으로 사용
- 큰 지역 면/타일이 지도를 가리지 않도록, 배경 지도 위에 작은 지역 버튼을 얹는 방식으로 변경
- 버튼 색상으로 선택 브랜드/지역 조건의 지표를 구분
- 오산·안성·연천·가평·이천·광주·여주·양주처럼 지금 분석 우선순위가 낮은 외곽 지역 버튼은 제외
- 서울 25개 구는 모두 클릭 가능하게 배치하고, 군포·용인 등 실사용 후보 경기 지역은 유지
- 색이 진할수록 선택 브랜드/조건에서 먼저 볼 만한 지역
- 붉은색은 최근 흐름 이상 신호가 강한 지역
- 지도 지역이나 오른쪽 목록의 `보기` 버튼을 누르면 해당 지역 후보로 필터링

다음에 더 다듬을 것:

- 현재는 배경 이미지 위에 클릭 가능한 분석 영역을 얹은 중간 단계입니다.
- 실제 행정구역 경계와 완전히 맞는 지도까지 필요하면 나중에 GeoJSON 또는 지도 라이브러리로 교체 검토
- 서울 구 단위, 경기 시 단위 좌표를 더 촘촘히 추가
- 저장 결과가 없을 때 지도 빈 상태 안내를 더 친절하게 만들기

### 3. 추가 키워드 결과를 별도 섹션으로 분리

현재 추가 키워드는 새 수집 때 분석에 포함된다는 상태 문구만 보입니다.

다음 개선안:

- `직접 입력 키워드` 섹션 추가
- 입력한 키워드별 검색량/추이/블로그 경쟁 표시
- 브랜드/지역 자동 추출이 애매한 키워드도 결과에서 확인 가능하게 만들기

### 4. 블로그 경쟁 판단 고도화

네이버 공식 API만으로는 블로그 지수, 실제 클릭 수, 구독자 수를 직접 가져오기 어렵습니다.

대신 가능한 대체 지표:

- 블로그 문서 총량
- 최근 글 비중
- 제목에 광고성 문구가 많은지
- 같은 블로그나 비슷한 제목 반복 여부
- 검색량 대비 문서량이 과한지

목표는 `블로그 경쟁 높음` 한 줄이 아니라 아래처럼 나누는 것입니다.

- 문서량 과다
- 광고성 제목 많음
- 검색량 대비 경쟁 낮음
- 제목 표본 부족

### 5. API 사용량 표시

여러 명이 쓸 수 있으므로 `새로 수집` 전에 예상 호출량을 보여주는 게 좋습니다.

다음 개선안:

- 이번 수집 예상 키워드 수
- SearchAd API 예상 호출 수
- Open API 예상 호출 수
- 현재 캐시 사용 여부
- 오늘 몇 시 이후 캐시인지

### 6. 검색 실행 방식과 버튼 UX 정리

2026-06-11 기준으로 1차 정리를 반영했습니다.

현재 방향:

- `분석하기` 버튼과 Enter 검색은 둘 다 유지
- `분석하기`와 Enter는 저장된 결과/캐시만 확인
- API를 새로 쓰는 행동은 `오늘 데이터 새로 수집` 버튼으로 분리
- `저장 데이터 보기` 버튼은 삭제
- `연결 상태 확인`은 `연결 상태 새로고침`으로 이름 변경
- 사이드바에 새 수집 예상 사용량 표시

현재 화면 안내:

- 저장 결과가 없으면 `저장 결과 없음`으로 표시
- 새 API 확인이 필요하면 `오늘 데이터 새로 수집`을 누르도록 안내
- 예상 사용량 예: `키워드 888개 · SearchAd 약 178회 · Open API 최대 420회`

다음에 더 고민할 것:

- `연결 상태 새로고침`도 별도 버튼으로 남겨둘지, 시작 점검 카드의 자동 갱신으로 흡수할지
- 새 수집 버튼을 누르기 전에 확인 문구나 작은 설명을 추가할지
- 예상 사용량이 너무 크게 보일 때 사용자가 겁먹지 않도록 설명 문구를 다듬을지
- 저장 결과가 없는 상태에서 그래프/카드가 너무 비어 보이지 않도록 빈 상태 UI를 더 친절하게 만들지

### 7. Windows 실행 확인

Windows 컴퓨터에서 이어받을 때 확인할 것:

- GitHub Desktop에서 Pull 후 저장소 루트가 `franchise_insight_agent`인지 확인
- `keyword-insight-agent/start-keyword-insight-agent.bat` 실행
- 브라우저에서 표시된 localhost 주소 접속
- `naver-api-config.json`은 GitHub에 올라가지 않으므로 Windows에도 별도로 준비 필요
- `naver-api-config.example.json`은 예시 파일일 뿐 실제 키가 아님

## 현재 변경 파일

- `keyword-insight-agent/insight-agent.html`
  - 검색창형 브랜드/지역 분석 UI
  - 검색량 상위/이상 신호 분리
  - 브랜드 수요 순위 제목 개선
  - 추가 키워드 상태 문구

- `naver-keyword-server.mjs`
  - 기본 브랜드/지역 목록 확장
  - 7일/30일 trend 데이터 저장
  - 검색량 상위와 이상 신호 데이터 생성
  - 검색 조건과 다른 캐시 fallback 제거

- `keyword-insight-agent/assets/capital-region-map-bg.png`
  - 수도권 지도 배경 이미지
  - 현재 지도 버튼 배치의 기준 이미지

## 검증한 내용

- 서버 문법 검사 통과
- 화면 로딩 확인
- 전체 기준에서 `브랜드 수요 순위 (24개 중 상위 10개)` 표시 확인
- `메가커피 / 서울 노원구` 조건 요청 시 해당 조건으로만 분석되는 것 확인
- 수도권 지도 배경 이미지 응답 확인
- 지도 버튼 46개 구성 확인
- 서울 25개 구 누락 없음 확인
- 오산·안성·연천·가평·이천·광주·여주·양주 버튼 제거 확인
- 군포·용인 버튼 유지 확인

## 커밋 전 주의

GitHub에 올리면 안 되는 파일:

- `naver-api-config.json`
- `keyword-cache*.json`
- `server.log`
- `.DS_Store`

현재 `.gitignore`에 들어가 있으므로 GitHub Desktop Changes 목록에 위 파일들이 보이면 커밋하지 말고 확인해야 합니다.

추천 커밋 문구:

```text
Refine keyword search workflow and insight signals
```

## 2026-06-25 작업 메모 - 검색량 정확도 + 점유공백/추세지속 통합

### 서버 업그레이드

- `matchMostSpecific` 헬퍼를 추가해 브랜드/지역 매칭 시 더 긴 후보를 우선 선택하도록 변경.
- `normalizeBrands`의 강제 브랜드 치환 로직을 제거해 입력한 브랜드명이 임의로 다른 브랜드로 바뀌지 않도록 정리.
- SearchAd 정확 매칭 실패 시 `브랜드 + 의도` 키워드로 2차 보정 조회를 추가.
- `searchVolumeStatus`를 `ok`, `fallback_brand`, `partial`, `unknown` 4단계로 구분.
- `opportunityScore`를 검색량 단독 중심에서 검색량, 블로그 문서 공백, 최근 추세, 양도양수 의도, 점유공백, 데이터 신뢰도 배율을 합산하는 구조로 변경.
- 상위 30개 후보에 한해 최근 블로그/카페 글을 조회하고 `틈새`, `따라쓰기`, `관찰`, `피하기` 상태를 붙임.
- 추천 결과 객체에 `competitionLabel`, `competitionReason`, `score`, `contentSamples`, 매칭 모호성 플래그를 내려보냄.

### 화면 업그레이드

- 기존 `insight-agent.html`에 남아 있던 충돌 표식과 깨진 한글을 제거하고 UTF-8 화면으로 재작성.
- 오늘 쓸 만한 키워드 추천 테이블에 검색량, 경쟁 상태, 신뢰도, 점수를 표시.
- `fallback_brand` 검색량은 숫자 옆에 `추정`으로 표시.
- 지도는 보조 패널로 유지하고 지역 클릭 시 해당 지역 후보를 필터링.
- 오른쪽에는 많이 검색된 브랜드 TOP 10, 최근 블로그/카페 광고글 TOP 10, 글쓰기 앱으로 넘길 복사 문구를 배치.

### 다음 확인할 것

- 실제 API 수집 시 `fallback_brand`가 너무 많이 나오면 SearchAd 보정 키워드 범위를 조정.
- `틈새/따라쓰기/관찰/피하기` 가중치가 실제 업무 판단과 맞는지 1~2주 사용 후 조정.
- 지도는 현재 SVG 기반이므로 실제 행정경계 지도가 필요하면 GeoJSON 방식으로 전환 검토.

## 2026-06-25 추가 메모 - SearchAd API 429 에러 수정

- SearchAd 키워드도구 호출에 `sleep(280)` 속도 제한을 추가.
- 호출 1회 단위로 `try/catch`를 적용해 429가 한 번 발생해도 전체 수집이 즉시 중단되지 않도록 수정.
- 429 발생 시 2초 추가 대기 후 다음 묶음을 시도.
- 연속 10회 실패하면 남은 후보는 건너뛰고, 그 시점까지 수집한 검색량 데이터는 유지.
- 응답 JSON에 `searchAdStats`를 추가해 후보 수, 정확 매칭 수, 보정 매칭 수, 429 횟수, 소요 시간을 확인할 수 있게 함.

## 2026-06-25 추가 메모 (네이버 검색 Open API 키 설정 문제 해결)

naver-api-config.json의 Open API 키 자체는 유효했으나, 네이버 개발자센터에서 해당 애플리케이션에 "검색" API 사용 설정이 빠져 있어 401 Authentication failed(errorCode 024)가 발생했던 것으로 확인됨. 개발자센터에서 "검색" API를 추가 등록한 뒤 정상화됨.

이로 인해 그동안 점유공백 분석(naverSearchItems)이 항상 빈 배열을 반환해 모든 키워드가 기계적으로 "틈새"로 잘못 표시되던 문제도 함께 해결됨.

최종 검증 중 Open API 호출도 동시에 많이 몰리면 429가 발생하는 것을 확인해 `naverSearchTotal`, `naverSearchItems`에 전역 요청 간격 제한을 추가함. 2026-06-25 기준 전체 새 수집 결과: 후보 216개, SearchAd 정확 매칭 216개, SearchAd 429 0회, 상위 30개 경쟁 라벨은 관찰 25개 / 따라쓰기 5개, 최근 블로그·카페 글 표본 20건 확인.
